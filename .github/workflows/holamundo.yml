name: hola mundo

# on:
#   push:
#     branches:
#       - main

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '*/30 * * * *' # cada 30 minutos
  push:
    branches:
      - main

jobs: 
  saludar:
    runs-on: ubuntu-latest
    steps: 
      - name: muestra mensaje 
        run: echo "hola mundo, soy Villaran"
